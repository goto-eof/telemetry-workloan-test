{
  "name": "telemetry",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "run:test": "turbo run dbms && turbo run containers --parallel && turbo run test",
    "run:rust:vs:rust": "turbo run rust_no_orm:write",
    "rust_no_orm": "turbo run test"
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {},
  "packageManager": "pnpm@7.19.0"
}
